# M3 verification and closure

Status: Accepted historical record  
Date: 2026-07-10  
Environment: Arch Linux, Hyprland/Wayland, GNU `stty` 9.11, Rust 1.96.0

- Nine tests passed, including 2,000-process collection, ancestry-cycle bounding, and rate units.
- Formatting, warnings-as-errors Clippy, and release build passed.
- Non-terminal `--tui` failed explicitly without emitting screen-control output.
- A pseudo-terminal run entered raw alternate-screen mode, refreshed, accepted `q`, exited successfully, and restored the terminal.
- Dimensions are refreshed and rendered rows are height-bounded.

Acceptance: complete-list default; four sort modes; three visible filter modes; ancestry/details; selection; resize handling; self marker; keyboard-only and color-independent operation.

Limitations: SIGKILL cannot be handled. Other fatal signals are not intercepted, so hard termination may require `stty sane`. Tree mode indents ancestry but does not impose depth-first ordering. Details expose commands. `stty` is required only for TUI mode.

M3 is complete without expanding into a GUI or compositor-specific process source.

