[package]
name = "whats-running"
version = "0.1.0"
edition = "2024"
description = "An honest, local-first Linux process snapshot"
license = "MIT OR Apache-2.0"

[dependencies]
iced = { version = "0.13", features = ["tokio", "canvas"] }

[profile.release]
strip = true
lto = true
codegen-units = 1
